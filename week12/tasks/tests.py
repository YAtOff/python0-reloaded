import doctest

import for_traverse
import while_traverse
import evens_count
import sum_numbers
import max_number
import min_number
import words_count
import head
import tail
import last
import take
import drop
import reverse

doctest.testmod(for_traverse)
doctest.testmod(while_traverse)
doctest.testmod(evens_count)
doctest.testmod(sum_numbers)
doctest.testmod(max_number)
doctest.testmod(min_number)
doctest.testmod(words_count)
doctest.testmod(head)
doctest.testmod(tail)
doctest.testmod(last)
doctest.testmod(take)
doctest.testmod(drop)
doctest.testmod(reverse)
